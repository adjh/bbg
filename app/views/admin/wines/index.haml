#content
  .caption
    %h1
      = t('admin.caption.wines')
      %span
        = link_to '添加酒品', new_admin_wine_path
        = link_to '导入虚拟分类', '#'
        = link_to '导入供应商', '#'
        = link_to '删除', '#'
  .body
    %ul.img_card_list.clearfix
      - 4.times do
        %li.wine.clearfix
          .avatar= image_tag('admin/wine-small.jpg', :size => '80x100')
          .meta
            %p wine name
            %p 209 RMB
#content_bot